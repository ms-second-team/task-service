CREATE TABLE IF NOT EXISTS epics (
    id BIGINT NOT NULL GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
    title VARCHAR(100) NOT NULL,
    deadline TIMESTAMP,
    executive_id BIGINT,
    event_id BIGINT NOT NULL
);